[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Legacy serverless payment routing only. SQLite auth requires running server.js on a host with persistent storage.
